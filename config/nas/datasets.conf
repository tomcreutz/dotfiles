# Non-secret configuration for the NAS datasets share.
# Credentials are generated locally by `./install.sh nas` and never stored in
# this repository.
NAS_DATASETS_SHARE="//192.168.178.2/datasets"
NAS_DATASETS_MOUNT_POINT="/mnt/nas-datasets"
NAS_DATASETS_CREDENTIALS_FILE="/etc/samba/nas-datasets.credentials"
NAS_DATASETS_SMB_VERSION="3.1.1"
