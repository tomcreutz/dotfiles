# Downloaded plugins (installed by setup scripts)
config/zellij/plugins/

# Generated by the CachyOS Hyprland migration for each machine
config/hypr/config/machine.lua
config/uwsm/env.local

# NAS credentials are generated locally and must never enter the repository
config/nas/*.credentials
config/nas/*.credentials.*
